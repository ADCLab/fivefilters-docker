# fivefilters-docker
A simple dockerfile setup used the in the process of deploying a self-hosted fivefilters using containerization.  The resulting image is availible on docker hub at __________.  

The dockerfile simple creates an Ubuntu image, runs the fivefilters updater to load required packages, then sets basic apache environmental variables.


# Docker Image
The resulting docker 

# Docker compose
